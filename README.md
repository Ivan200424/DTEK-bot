# GraphenkoBot (@DTEK-bot) 🔌⚡

Telegram бот для автоматичного оновлення графіків відключень електроенергії та моніторингу електропостачання в Україні.

## 🌟 Основні функції

### 📊 Graphenko - Автоматичне оновлення графіків
- Оновлює закріплене повідомлення із PNG-зображенням кожні 5 хвилин
- Підтримка одиночних зображень або пари (сьогодні/завтра)
- Автоматичне додавання timestamp у форматі Europe/Kyiv
- Cache-busting для обходу кешу Telegram

### ⚡ Моніторинг електропостачання
- Автоматичний моніторинг через TCP-перевірку роутера (IP: 93.127.118.86, порт: 443)
- Перевірка кожні 30 секунд
- Миттєві сповіщення про зникнення та повернення світла
- Показ тривалості простою/роботи
- Інтеграція з графіком планових відключень

## 🤖 Команди бота

**Графіки відключень:**
- `/graphenko_image <url.png>` — встановити URL зображення для оновлень
- `/graphenko_caption <текст>` — встановити власний підпис
- `/graphenko_caption -default` — повернути стандартний підпис

**Моніторинг електропостачання:**
- `/monitor_on` — увімкнути моніторинг роутера (пінг 93.127.118.86:443)
- `/monitor_off` — вимкнути моніторинг
- `/monitor_status` — перевірити статус моніторингу

## 📋 Вимоги

- **Python** 3.7 або новіше
- **BOT_TOKEN** (змінна середовища)
- Бот має бути адміністратором із правом закріплення та видалення повідомлень

## 🚀 Швидкий старт

### Крок 1: Створення бота

1. Створіть бота через [@BotFather](https://t.me/BotFather):
   ```
   /newbot
   ```
2. Дайте ім'я боту та збережіть отриманий **Bot Token**

### Крок 2: Додавання бота до каналу

1. Додайте бота до вашого каналу/чату як адміністратора
2. Надайте боту права:
   - ✅ Pin messages
   - ✅ Delete messages
   - ✅ Post messages

### Крок 3: Налаштування зображення

У каналі надішліть команду з URL зображення:

```
/graphenko_image https://raw.githubusercontent.com/Baskerville42/outage-data-ua/refs/heads/main/images/kyiv/gpv-3-2-emergency.png
```

### Крок 4: Увімкнення моніторингу

```
/monitor_on
```

## 🔧 Локальний запуск

1. Встановіть залежності:
   ```bash
   pip install -r requirements.txt
   ```

2. Створіть `.env` файл або встановіть змінну середовища:
   ```bash
   export BOT_TOKEN=your_telegram_bot_token_here
   ```

3. Запустіть бота:
   ```bash
   python3 bot.py
   ```

## ☁️ Deployment на Railway

Railway - це Platform-as-a-Service (PaaS) для deploy Python додатків 24/7.

### Крок 1: Підготовка репозиторію

Переконайтеся, що у вашому репозиторії є:
- `bot.py` — головний файл бота
- `requirements.txt` — залежності Python
- `Procfile` — інструкції для запуску
- `graphenko-chats.json` — конфігурація (створюється автоматично)

### Крок 2: Deploy на Railway

1. Зайдіть на [railway.app](https://railway.app)
2. Увійдіть через GitHub
3. Натисніть **"New Project"** → **"Deploy from GitHub repo"**
4. Виберіть ваш репозиторій `DTEK-bot`
5. Railway автоматично виявить Python проект

### Крок 3: Налаштування змінних середовища

1. Відкрийте ваш проект на Railway
2. Перейдіть у вкладку **"Variables"**
3. Додайте змінну:
   - **Name:** `BOT_TOKEN`
   - **Value:** ваш токен від BotFather

### Крок 4: Deploy

Railway автоматично розгорне бота після commit у main гілку.

Переваги Railway:
- ✅ Безкоштовний план (500 годин/місяць)
- ✅ Автоматичний deploy при push
- ✅ Перезапуск при збоях
- ✅ Логи в реальному часі

## 🖥️ Deployment на VPS (Ubuntu/Debian)

Для постійного 24/7 запуску на власному сервері використовуйте systemd.

### Крок 1: Підготовка сервера

```bash
# Оновлення системи
sudo apt update && sudo apt upgrade -y

# Встановлення Python та Git
sudo apt install -y python3 python3-pip git

# Клонування репозиторію
cd /opt
sudo git clone https://github.com/Ivan200424/DTEK-bot.git
cd DTEK-bot

# Встановлення залежностей
sudo pip3 install -r requirements.txt
```

### Крок 2: Створення systemd service

Створіть файл `/etc/systemd/system/dtek-bot.service`:

```ini
[Unit]
Description=DTEK Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/DTEK-bot
Environment="BOT_TOKEN=your_bot_token_here"
ExecStart=/usr/bin/python3 /opt/DTEK-bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Важливо:** Замініть `your_bot_token_here` на ваш реальний токен.

### Крок 3: Запуск сервісу

```bash
# Перезавантаження systemd
sudo systemctl daemon-reload

# Увімкнення автозапуску
sudo systemctl enable dtek-bot

# Запуск бота
sudo systemctl start dtek-bot

# Перевірка статусу
sudo systemctl status dtek-bot
```

### Крок 4: Керування ботом

```bash
# Перезапуск
sudo systemctl restart dtek-bot

# Зупинка
sudo systemctl stop dtek-bot

# Перегляд логів
sudo journalctl -u dtek-bot -f

# Перегляд останніх 100 рядків логів
sudo journalctl -u dtek-bot -n 100
```

### Крок 5: Оновлення бота

```bash
cd /opt/DTEK-bot
sudo git pull
sudo systemctl restart dtek-bot
```

## 📁 Структура проекту

```
DTEK-bot/
├── bot.py                   # Головний файл бота (Python)
├── requirements.txt         # Залежності Python
├── Procfile                 # Конфігурація для Railway
├── graphenko-chats.json     # Конфігурація каналів (автоматично)
├── .env.example             # Приклад змінних середовища
├── README.md                # Документація
└── .gitignore               # Ігноровані файли
```

## 🔒 Безпека

- ❌ **Ніколи** не комітьте `.env` файл з токенами
- ✅ Використовуйте змінні середовища для секретів
- ✅ Обмежте доступ до `graphenko-chats.json`
- ✅ Регулярно оновлюйте залежності

## 🐛 Troubleshooting

### Бот не відповідає на команди

1. Перевірте, чи бот запущений:
   ```bash
   sudo systemctl status dtek-bot
   ```

2. Перегляньте логи на помилки:
   ```bash
   sudo journalctl -u dtek-bot -n 50
   ```

3. Переконайтеся, що `BOT_TOKEN` встановлено правильно

### Моніторинг не працює

1. Перевірте, чи увімкнено моніторинг:
   ```
   /monitor_status
   ```

2. Переконайтеся, що хост доступний:
   ```bash
   nc -zv 93.127.118.86 443
   ```

### Graphenko не оновлюється

1. Перевірте, чи встановлено `image_url`:
   ```
   /graphenko_image <url>
   ```

2. Переконайтеся, що URL доступний та є PNG зображенням

## ⚡️ Міграція з GitHub Actions

Якщо ви мігруєте з попередньої версії на GitHub Actions:

1. ✅ Всі дані з `graphenko-chats.json` збережені
2. ✅ Обидві функції (Graphenko + моніторинг) працюють в одному боті
3. ✅ Команди залишилися незмінними
4. ✅ GitHub Actions workflows більше не потрібні

**Зміни:**
- ❌ Видалено `.github/workflows/`
- ❌ Видалено `index.mjs`, `monitor-worker.mjs`, `src/`
- ❌ Видалено `package.json`, `package-lock.json`
- ✅ Додано `bot.py` (Python standalone)
- ✅ Додано `requirements.txt`, `Procfile`

## 📊 Моніторинг та логи

### Railway
Логи доступні в dashboard Railway у реальному часі.

### VPS (systemd)
```bash
# Реальний час
sudo journalctl -u dtek-bot -f

# Останні N рядків
sudo journalctl -u dtek-bot -n 100

# З певної дати
sudo journalctl -u dtek-bot --since "2024-01-01"
```

## 🤝 Contribution

Pull requests вітаються! Для великих змін спочатку відкрийте issue.

## 📝 Ліцензія

MIT

---

**Слава Україні! 🇺🇦**
