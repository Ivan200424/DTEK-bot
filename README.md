# DTEK Power Monitoring Bot 🔌⚡

Telegram бот для моніторингу наявності електроенергії через пінг роутера. Автоматично відстежує зміни статусу світла та надсилає сповіщення в Telegram канал.

## 🌟 Основні функції

### 1. **Моніторинг наявності світла**
- Автоматичний пінг IP-адреси роутера кожні 5 хвилин (налаштовується)
- Визначення статусу світла на основі результату пінгу
- Збереження історії змін у `light-history.json`

### 2. **Сповіщення в Telegram**
Бот надсилає повідомлення при зміні статусу:

**Коли світло зникло:**
```
🔴 Світло зникло
🕐 О 14:30 (21.01)
⏱ Було світла 2г 15хв
```

**Коли світло з'явилося:**
```
🟢 Світло з'явилось
🕐 О 16:45 (21.01)
⏱ Його не було 2г 15хв
```

### 3. **Моніторинг графіків відключень**
- Автоматичне завантаження даних з https://github.com/Baskerville42/outage-data-ua
- Парсинг JSON для вашого регіону та групи
- Сповіщення при оновленні графіку відключень
- Команда `/schedule` для перегляду поточного графіку
- Всі зміни зберігаються у `light-history.json`
- Містить час події, тип події (ON/OFF) та тривалість
- Зберігається до 100 останніх записів
- Можливість експорту в CSV формат

### 5. **Команди бота**
- `/start` - Почати роботу з ботом
- `/light_history` - Переглянути останні 10 змін
- `/status` - Поточний статус світла
- `/schedule` - Переглянути графік відключень
- `/export` - Експортувати історію в CSV
- `/help` - Довідка по командам

### 6. **Автоматизація через GitHub Actions**
- Автоматичний запуск кожні 5 хвилин
- Можливість ручного запуску через інтерфейс GitHub
- Автоматичне збереження історії змін в репозиторій

## 📋 Вимоги

- **Node.js** 18 або новіше
- **Telegram Bot Token** (отримати у [@BotFather](https://t.me/BotFather))
- **GitHub Actions** (для автоматичного моніторингу)

## 🚀 Швидкий старт

### Режим 1: Автоматичний моніторинг (GitHub Actions)

Для автоматичного моніторингу через GitHub Actions:

### Крок 1: Налаштування Telegram бота

1. Створіть бота через [@BotFather](https://t.me/BotFather):
   - Відправте `/newbot`
   - Дайте ім'я боту
   - Збережіть отриманий **Bot Token**

2. Отримайте Chat ID вашого каналу:
   - Додайте бота до вашого каналу як адміністратора
   - Відправте повідомлення в канал
   - Відкрийте `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Знайдіть `chat.id` у відповіді

### Крок 2: Налаштування репозиторію

1. Форкніть цей репозиторій
2. Перейдіть в **Settings** → **Secrets and variables** → **Actions**
3. Додайте секрет:
   - **Name:** `TELEGRAM_BOT_TOKEN`
   - **Value:** ваш токен від BotFather

### Крок 3: Налаштування конфігурації

Відредагуйте файл `light-config.json`:

```json
{
  "chat_id": "-1003523279109",
  "ip": "93.127.118.86",
  "region": "kyiv-region",
  "group": "3.1",
  "ping_interval": 60000,
  "ping_timeout": 5
}
```

**Параметри:**
- `chat_id` - ID вашого Telegram каналу (з мінусом для каналів)
- `ip` - IP-адреса роутера для пінгу
- `region` - регіон для моніторингу графіків відключень
- `group` - група GPV
- `ping_interval` - інтервал між перевірками (мс)
- `ping_timeout` - таймаут пінгу (секунди)

### Крок 4: Запуск

#### Автоматичний запуск (GitHub Actions)

Workflow автоматично запускається кожні 5 хвилин. Налаштування у `.github/workflows/monitor.yml`:

```yaml
on:
  schedule:
    - cron: '*/5 * * * *'  # Кожні 5 хвилин
  workflow_dispatch:       # Ручний запуск
```

Для ручного запуску:
1. Перейдіть у **Actions** → **DTEK Power Monitoring**
2. Натисніть **Run workflow**
3. Виберіть гілку та натисніть **Run workflow**

### Режим 2: Інтерактивний бот (локально)

Для запуску бота з підтримкою команд локально:

```bash
# Встановити залежності
npm install

# Скопіювати приклад .env файлу
cp .env.example .env

# Відредагувати .env та додати ваш TELEGRAM_BOT_TOKEN
nano .env

# Запустити інтерактивного бота
npm run bot
```

Після запуску бот буде слухати команди від користувачів у Telegram.

### Режим 3: Одноразова перевірка

```bash
# Встановити залежності
npm install

# Встановити змінну оточення (опціонально)
export TELEGRAM_BOT_TOKEN="your_bot_token_here"

# Виконати одну перевірку
npm start
```

Виконає одну перевірку статусу та завершить роботу.

## 📁 Структура проекту

```
DTEK-bot/
├── .github/
│   └── workflows/
│       └── monitor.yml       # GitHub Actions workflow
├── index.js                  # Основний код моніторингу
├── bot.js                    # Інтерактивний бот з командами
├── outage-monitor.js         # Модуль моніторингу графіків
├── package.json              # Залежності проекту
├── light-config.json         # Конфігурація
├── light-history.json        # Історія змін (автоматично)
├── outage-schedule.json      # Збережений графік (автоматично)
├── light-history.csv         # Експорт історії (опціонально)
├── .env.example              # Приклад файлу змінних оточення
├── .gitignore               # Ігноровані файли
└── README.md                # Ця документація
```

## 🔧 Розширені налаштування

### Зміна частоти моніторингу

Відредагуйте `.github/workflows/monitor.yml`:

```yaml
schedule:
  - cron: '*/1 * * * *'  # Кожну хвилину
  - cron: '*/10 * * * *' # Кожні 10 хвилин
  - cron: '0 * * * *'    # Кожну годину
```

**Увага:** Занадто часті запуски можуть призвести до обмеження GitHub Actions.

### Формат історії

Файл `light-history.json` має структуру:

```json
{
  "current_status": "ON",
  "last_change": "2026-01-21T18:55:00.000Z",
  "history": [
    {
      "time": "2026-01-21T18:55:00.000Z",
      "event": "ON",
      "duration": 7200000,
      "duration_formatted": "2г 0хв"
    }
  ]
}
```

### Експорт в CSV

Історія автоматично експортується в CSV формат при виклику відповідної функції:

```csv
Time,Event,Duration (ms),Duration Formatted
2026-01-21T18:55:00.000Z,ON,7200000,2г 0хв
```

## 📊 Моніторинг графіків відключень

Бот автоматично завантажує та моніторить графіки відключень з офіційного джерела.

### Як це працює:

1. Кожного запуску бот перевіряє оновлення графіку для вашого регіону та групи
2. Якщо графік оновився, надсилається сповіщення в канал
3. Використовуйте команду `/schedule` для перегляду поточного графіку

### Налаштування:

У `light-config.json` вкажіть:
- `region` - ваш регіон (наприклад, `kyiv-region`, `lviv-region`)
- `group` - ваша група GPV (наприклад, `3.1`, `2.2`)

Дані завантажуються з https://github.com/Baskerville42/outage-data-ua

## 🐛 Усунення неполадок

### Бот не надсилає повідомлення

1. Перевірте, чи правильно встановлено `TELEGRAM_BOT_TOKEN`
2. Перевірте, чи бот доданий до каналу як адміністратор
3. Перевірте правильність `chat_id` у конфігурації

### Не працює пінг

1. Перевірте доступність IP-адреси роутера
2. Збільште `ping_timeout` у конфігурації
3. Перевірте логи в Actions → Run details

### GitHub Actions не запускається

1. Перевірте, чи увімкнені Actions у вашому репозиторії
2. Перевірте синтаксис YAML файлу
3. Переконайтеся, що у вас є доступ до Actions

## 📝 Логи

Переглянути логи виконання:
1. Перейдіть у **Actions**
2. Виберіть workflow run
3. Відкрийте **monitor** job
4. Розгорніть кроки для перегляду деталей

## 🤝 Внесок

Ви можете допомогти покращити проект:
1. Fork репозиторію
2. Створіть гілку для ваших змін
3. Внесіть зміни та зробіть commit
4. Створіть Pull Request

## 📄 Ліцензія

MIT License - використовуйте вільно!

## ⚠️ Важливо

- Не публікуйте ваш `TELEGRAM_BOT_TOKEN` у відкритому доступі
- Регулярно перевіряйте правильність роботи бота
- Історія обмежена 100 записами для економії місця
- GitHub Actions має обмеження на кількість запусків

## 📞 Підтримка

Якщо у вас виникли питання або проблеми:
1. Перевірте розділ "Усунення неполадок"
2. Перегляньте Issues у репозиторії
3. Створіть новий Issue з детальним описом проблеми

---

**Слава Україні! 🇺🇦**
